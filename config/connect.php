<?php
    $HOST = 'localhost';
    $USER = 'root';
    $PASSWORD = '';
    $DB = 'social-network';

    $conn = mysqli_connect($HOST, $USER, $PASSWORD, $DB);
    // if($conn) echo('DB Connected!');
?>